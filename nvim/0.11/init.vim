
" Use the same path as for vim (so that pack/* works)
set packpath^=~/.vim
set packpath+=~/.vim/after

" Use the same runtimepath as Vim (so that colors/ and so on is found)
set runtimepath^=~/.vim
set runtimepath+=~/.vim/after

" Read your regular vimrc
source ~/.vimrc

